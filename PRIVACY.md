# CRIS 隐私说明

## 默认行为

完成测验时，50 题答案保存在当前浏览器内存中。若启用了“匿名加入群体密度图”，答案会通过 HTTPS 发送到云函数，由服务端重新计算 M/F 分数并执行基础作答质量检查；随后只累计到 0.25 分的二维分箱计数中，原始答案不会写入群体统计数据库。

## 私人云备份

完整 50 题答案仅在用户主动点击“创建私人云备份 + 恢复码”后保存。

恢复凭证由浏览器使用密码学安全随机数生成。服务器只保存其 SHA-256 哈希作为索引，不保存明文恢复凭证。分享 JSON、群体统计和报告截图都不包含恢复凭证。

持有恢复码的人可以读取或删除对应私人备份，因此恢复码应当像密码一样保管。

## 删除

“删除私人云备份”会删除可恢复的个人原始答案记录。匿名群体统计是已经不可逆聚合的分箱计数，不保存逐人记录，因此不能再从聚合计数中识别并单独移除某个参与者。

## IP 与平台日志

CRIS 应用数据库不保存 IP。云函数为了基础限流会在实例内存中短暂处理请求来源 IP，不写入应用数据库。腾讯云 / HTTP 网关仍可能按平台策略产生基础访问日志；因此不要使用“系统从不处理 IP”这种绝对表述。

## 旧版数据

旧版应用曾以 `uid` 作为恢复凭证并保存于 `responses` 集合。v2 后端保留旧恢复码兼容读取 / 删除能力，迁移完成后应按部署文档处理旧集合。
